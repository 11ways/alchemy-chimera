<%
text_value = data.value == null ? '' : data.value.stripTags().truncate(100);

if (!text_value) {
	text_value = createElement('i');
	text_value.append(__d('chimera', 'empty'));
}

%>
<%= text_value %>